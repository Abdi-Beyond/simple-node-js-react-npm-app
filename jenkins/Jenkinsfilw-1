pipeline {
    agent {
        docker {
            image 'node:alpine3.21'
            label 'docker-agent'
        }
    }
    stages {
        stage('builder') {
            steps {
                sh 'docker --version'  // First command to check Docker version
                sh 'node --version'    // Second command to check Node.js version
            }
        }
    }
}
