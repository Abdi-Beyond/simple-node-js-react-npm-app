pipeline {
    agent {
        label 'docker-agent'
    }
    stages {
        stage('builder') {
            
            steps {
                sh 'docker pull node'
                sh 'node --version'
            }
        }
    }
}
