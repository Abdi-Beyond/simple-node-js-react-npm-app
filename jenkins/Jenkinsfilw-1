pipeline {
    agent {
        docker {
            image 'node:alpine3.21'
            label 'docker-agent'
        }
    }
    stages {
        stage('builder') {
            steps {
                sh 'node --version'    // Second command to check Node.js version
            }
        }
    }
}
